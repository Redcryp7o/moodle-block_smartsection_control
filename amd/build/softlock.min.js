/**
 * AMD module: SmartSection Control — Soft Lock teaser presentation.
 *
 * Applies the ssc-softlock class to the course-view elements belonging to
 * sections that are Soft Locked for the current user. The teaser appearance
 * itself is declared statically in the plugin styles.css, so this module only
 * toggles a class and never injects a stylesheet or inline styles.
 *
 * Access control is enforced server-side by Moodle section availability; this
 * module is presentation only.
 *
 * @module     block_smartsection_control/softlock
 * @copyright  2026 M. AFZAL RIAZ
 * @license    https://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define("block_smartsection_control/softlock",[],(function(){function applyTeaser(sections){sections.forEach((function(section){(function(section){var sectionnum=parseInt(section.sectionnum,10),sectionid=parseInt(section.sectionid,10),selectors=[];return isNaN(sectionnum)||(selectors.push("#section-"+sectionnum),selectors.push("li#section-"+sectionnum),selectors.push('[data-sectionreturnid="'+sectionnum+'"]')),isNaN(sectionid)||selectors.push('[data-sectionid="'+sectionid+'"]'),selectors})(section).forEach((function(selector){document.querySelectorAll(selector).forEach((function(element){element.classList.add("ssc-softlock")}))}))}))}return{init:function(sections){Array.isArray(sections)&&0!==sections.length&&("loading"!==document.readyState?applyTeaser(sections):document.addEventListener("DOMContentLoaded",(function(){applyTeaser(sections)})))}}}));

//# sourceMappingURL=softlock.min.js.map